<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="CheckBoxStyle" TargetType="{x:Type CheckBox}">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Height" Value="25" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource dgFocusVisualStyte}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <BulletDecorator>
                        <BulletDecorator.Bullet>
                            <Grid Height="{TemplateBinding Height}" Width="{Binding RelativeSource={RelativeSource Self},
                                Path=Height, UpdateSourceTrigger=PropertyChanged}"
                              MinHeight="25" MinWidth="25" ShowGridLines="False">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="4*" />
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="4*" />
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="2*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="3*" />
                                    <RowDefinition Height="1*" />
                                    <RowDefinition Height="1*" />
                                    <RowDefinition Height="1*" />
                                    <RowDefinition Height="4*" />
                                    <RowDefinition Height="1*" />
                                    <RowDefinition Height="1*" />
                                    <RowDefinition Height="4*" />
                                </Grid.RowDefinitions>

                                <Border Name="MainBorder"
                                    Grid.ColumnSpan="9" Grid.RowSpan="9"
                                    CornerRadius="4"
                                    BorderThickness="1"
                                    Background="Transparent" />

                                <Border Name="InnerBorder"
                                    Grid.Column="1" Grid.ColumnSpan="5"
                                    Grid.Row="2" Grid.RowSpan="5"
                                    BorderThickness="1"
                                    BorderBrush="DarkGray" />

                                <Path Name="InnerPath"
                                  Grid.Column="1" Grid.ColumnSpan="5"
                                  Grid.Row="2" Grid.RowSpan="5"
                                  Data="M31,5 L19.5,5 19.5,19.5 34.5,19.5 34.5,11.75"
                                  Stretch="Fill" Stroke="DarkGray"/>

                                <Path Name="CheckMark"
                                  Grid.Column="2" Grid.ColumnSpan="5"
                                  Grid.Row="1" Grid.RowSpan="5"
                                  Opacity="0"
                                  Data="M9.07743946676476E-09,4.31805768640244L4.68740335877841,8.86361158398516C4.68740335877841,8.86361158398516,16.3281249985376,-2.42451336648723,16.3281249985376,-2.42451336648723L14.0622100581796,-4.77304938341948 4.68740335877846,4.31805791992662 2.22656251699567,1.93164208562579z"
                                  Fill="Black"
                                  Stretch="Fill"
                                  />

                                <Path Name="InderminateMark"
                                  Grid.Column="3"
                                  Grid.Row="4"
                                  Data="M0,4 L1,5 5,1 4,0"
                                  Opacity="0"
                                  Stretch="Fill"
                                  StrokeThickness="0"
                                  Fill="DarkGray" />
                            </Grid>
                        </BulletDecorator.Bullet>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CheckStates">
                                <VisualState x:Name="Checked">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             Storyboard.TargetName="CheckMark" Duration="0:0:0.2" To="1" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Unchecked" >
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             Storyboard.TargetName="CheckMark" Duration="0:0:0.2" To="0" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Indeterminate">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             Storyboard.TargetName="InderminateMark" Duration="0:0:0.2" To="1" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <ContentPresenter Margin="4,0,4,0"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        RecognizesAccessKey="True" />
                    </BulletDecorator>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="InnerBorder" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" Value="Gray" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="CheckMark" Property="Fill" Value="DarkGray" />
                            <Setter TargetName="CheckMark" Property="Stroke" Value="DarkGray" />
                            <Setter TargetName="InnerPath" Property="Stroke" Value="DarkGray" />
                            <Setter TargetName="InderminateMark" Property="Fill" Value="DarkGray" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="DarkGray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Custom CheckBox Style -->
    <Style x:Key="CustomCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5,2,5,2"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Checkbox Container -->
                        <Grid Width="15" Height="15">
                            <!-- Background and Border -->
                            <Border x:Name="CheckBorder"
                                  Width="15" Height="15"
                                  CornerRadius="2"
                                  BorderThickness="1"
                                  BorderBrush="#00bcb4"
                                  Background="White"/>

                            <!-- Check Background (Appears when Checked) -->
                            <Border x:Name="CheckBackground"
                                  Width="15" Height="15"
                                  CornerRadius="2"
                                  Background="Transparent"
                                  Visibility="Collapsed"/>

                            <!-- Centered Checkmark Path -->
                            <Viewbox Width="12" Height="12" Visibility="Collapsed" x:Name="Checkmark">
                                <Path Data="M 2 6 L 6 10 L 12 2"
                                    Stroke="White"
                                    StrokeThickness="2"
                                    StrokeStartLineCap="Round"
                                    StrokeEndLineCap="Round"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                            </Viewbox>
                        </Grid>

                        <!-- Checkbox Label -->
                        <TextBlock Text="{TemplateBinding Content}"
                                 Foreground="Black"
                                 FontSize="12"
                                 VerticalAlignment="Center"
                                 Margin="5,0,0,0"/>
                    </StackPanel>

                    <!-- Triggers for Checked State -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckBackground" Property="Background" Value="#00bcb4"/>
                            <Setter TargetName="CheckBackground" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="Checkmark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="NACheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5,2,5,2"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Checkbox Container -->
                        <Grid Width="18" Height="18">
                            <!-- Background and Border -->
                            <Border x:Name="CheckBorder"
                                  Width="18" Height="18"
                                  CornerRadius="2"
                                  BorderThickness="2"
                                  BorderBrush="LightGray"
                                  Background="White"/>

                            <!-- Check Background (Appears when Checked) -->
                            <Border x:Name="CheckBackground"
                                  Width="16" Height="16"
                                  CornerRadius="2"
                                  Background="Transparent"
                                  Visibility="Collapsed"/>

                            <!-- Centered Checkmark Path -->
                            <Viewbox Width="12" Height="12" Visibility="Collapsed" x:Name="Checkmark">
                                <Path Data="M 2 6 L 6 10 L 12 2"
                                    Stroke="White"
                                    StrokeThickness="2"
                                    StrokeStartLineCap="Round"
                                    StrokeEndLineCap="Round"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                            </Viewbox>
                        </Grid>

                        <!-- Checkbox Label -->
                        <TextBlock Text="{TemplateBinding Content}"
                                 Foreground="LightGray"
                                 FontSize="12"
                                 VerticalAlignment="Center"
                                 Margin="5,0,0,0"/>
                    </StackPanel>

                    <!-- Triggers for Checked State -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckBackground" Property="Background" Value="LightGray"/>
                            <Setter TargetName="CheckBackground" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="Checkmark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CategoryCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5,2,5,2"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Checkbox Container -->
                        <Grid Width="25" Height="25">
                            <!-- Background and Border -->
                            <Border x:Name="CheckBorder"
                              Width="25" Height="25"
                              CornerRadius="2"
                              BorderThickness="2"
                              BorderBrush="#00bcb4"
                              Background="White"/>

                            <!-- Check Background (Appears when Checked) -->
                            <Border x:Name="CheckBackground"
                              Width="23" Height="23"
                              CornerRadius="2"
                              Background="Transparent"
                              Visibility="Collapsed"/>

                            <!-- Centered Checkmark Path -->
                            <Viewbox Width="18" Height="18" Visibility="Collapsed" x:Name="Checkmark">
                                <Path Data="M 2 6 L 6 10 L 12 2"
                                Stroke="White"
                                StrokeThickness="2"
                                StrokeStartLineCap="Round"
                                StrokeEndLineCap="Round"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                            </Viewbox>
                        </Grid>

                        <!-- Checkbox Label -->
                        <TextBlock Text="{TemplateBinding Content}"
                                 Foreground="Black"
                                 FontSize="18"
                                 FontWeight="SemiBold"
                                 VerticalAlignment="Center"
                                 Margin="5,0,0,0"/>
                    </StackPanel>

                    <!-- Triggers for Checked State -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckBackground" Property="Background" Value="#00bcb4"/>
                            <Setter TargetName="CheckBackground" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="Checkmark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="NACategoryCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5,2,5,2"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Checkbox Container -->
                        <Grid Width="25" Height="25">
                            <!-- Background and Border -->
                            <Border x:Name="CheckBorder"
                              Width="25" Height="25"
                              CornerRadius="2"
                              BorderThickness="2"
                              BorderBrush="LightGray"
                              Background="White"/>

                            <!-- Check Background (Appears when Checked) -->
                            <Border x:Name="CheckBackground"
                              Width="23" Height="23"
                              CornerRadius="2"
                              Background="Transparent"
                              Visibility="Collapsed"/>

                            <!-- Centered Checkmark Path -->
                            <Viewbox Width="18" Height="18" Visibility="Collapsed" x:Name="Checkmark">
                                <Path Data="M 2 6 L 6 10 L 12 2"
                                Stroke="White"
                                StrokeThickness="2"
                                StrokeStartLineCap="Round"
                                StrokeEndLineCap="Round"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                            </Viewbox>
                        </Grid>

                        <!-- Checkbox Label -->
                        <TextBlock Text="{TemplateBinding Content}"
                                 Foreground="LightGray"
                                 FontSize="18"
                                 FontWeight="SemiBold"
                                 VerticalAlignment="Center"
                                 Margin="5,0,0,0"/>
                    </StackPanel>

                    <!-- Triggers for Checked State -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckBackground" Property="Background" Value="LightGray"/>
                            <Setter TargetName="CheckBackground" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="Checkmark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CustomCheckBoxStyle1" TargetType="CheckBox">
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Checkbox Container -->
                        <Grid Width="15" Height="15">
                            <!-- Background and Border -->
                            <Border x:Name="CheckBorder"
                              Width="15" Height="15"
                              CornerRadius="2"
                              BorderThickness="1"
                              BorderBrush="Gray"
                              Background="White"/>

                            <!-- Check Background (Appears when Checked) -->
                            <Border x:Name="CheckBackground"
                              Width="15" Height="15"
                              CornerRadius="2"
                              Background="Transparent"
                              Visibility="Collapsed"/>

                            <!-- Centered Checkmark Path -->
                            <Viewbox Width="12" Height="12" Visibility="Collapsed" x:Name="Checkmark">
                                <Path Data="M 2 6 L 6 10 L 12 2"
                                Stroke="White"
                                StrokeThickness="2"
                                StrokeStartLineCap="Round"
                                StrokeEndLineCap="Round"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                            </Viewbox>
                        </Grid>

                        <!-- Checkbox Label -->
                        <TextBlock Text="{TemplateBinding Content}"
                             Foreground="Black"
                             FontSize="12"
                             VerticalAlignment="Center"
                             Margin="5,0,0,0"/>
                    </StackPanel>

                    <!-- Triggers for Checked State -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckBackground" Property="Background" Value="#00bcb4"/>
                            <Setter TargetName="CheckBackground" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="Checkmark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>